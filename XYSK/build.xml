<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. --><project basedir="." default="war" name="XYSK">
    <property environment="env"/>
    <property name="TOMCAT_HOME" value="../../tomcat/9"/>
    <property name="debuglevel" value="source,lines,vars"/>
    <property name="target" value="11"/>
    <property name="source" value="11"/>
	<property name="javac_dir" value="javac"/>
    <property name="deploy_dir" value="deploy"/>
    <property name="war" value="war"/>
    <property name="temp" value="tools"/>
    <property name="settings" value="setting"/>
    <path id="XYSK.classpath">
        <pathelement location="${TOMCAT_HOME}/lib/servlet-api.jar"/>
        <pathelement location="${TOMCAT_HOME}/lib/jasper.jar"/>
        <pathelement location="${TOMCAT_HOME}/lib/jsp-api.jar"/>
        <pathelement location="${TOMCAT_HOME}/lib/el-api.jar"/>
        <pathelement location="${TOMCAT_HOME}/lib/annotations-api.jar"/>
        <pathelement location="WEB-INF/classes"/>
        <pathelement location="work"/>
        <pathelement location="WEB-INF/lib/orai18n-23.2.0.0.jar"/>
        <pathelement location="WEB-INF/lib/ojdbc14.jar"/>
    </path>
    <target name="init">
        <mkdir dir="WEB-INF/classes"/>
        <mkdir dir="work"/>
        <copy includeemptydirs="false" todir="WEB-INF/classes">
            <fileset dir="WEB-INF/src">
                <exclude name="**/*.launch"/>
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
        <copy includeemptydirs="false" todir="work">
            <fileset dir="work">
                <exclude name="**/*.launch"/>
                <exclude name="**/*.java"/>
            </fileset>
        </copy>
    </target>

    <target name="copy_src">
        <!-- java_comp_dir -->
        <delete dir="${javac_dir}"/>
        <mkdir dir="${javac_dir}/WEB-INF/classes"/>
        <!-- war_dir -->
        <delete dir="${deploy_dir}"/>
        <mkdir dir="${deploy_dir}/WEB-INF/classes"/>

        <!-- parts copy -->
        <copy todir="${deploy_dir}/WEB-INF/lib" overwrite="true">
        <fileset dir="${basedir}/WEB-INF/lib">
        <include name="*.jar"/>
        </fileset>
        </copy>
    	
        <copy todir="${deploy_dir}/WEB-INF" overwrite="true">
            <fileset dir="${basedir}/WEB-INF" includes="*.xml" />
        </copy>
    	
        <copy todir="${deploy_dir}/contdata" overwrite="true">
            <fileset dir="${basedir}/contdata" includes="**/*" />
        </copy>
    	
        <copy todir="${deploy_dir}" overwrite="true">
            <fileset dir="${basedir}" includes="*.jsp" />
        </copy>
    	
        <copy todir="${deploy_dir}" overwrite="true">
            <fileset dir="${basedir}" includes="*.css" />
        </copy>

        <copy includeemptydirs="false" 
            todir="${javac_dir}" overwrite="true">
            <fileset dir="${basedir}/WEB-INF">
            	<include name="**/*.java"/>
            </fileset>
        </copy>
    	
    	<copy includeemptydirs="false" 
            todir="${javac_dir}/src/xysk" overwrite="true">
            <fileset dir="${settings}">
                <!--include name="xzlm.properties"/-->
            	<include name="Xysflj_Common.java"/>
            </fileset>
        </copy>
    	
        </target>

        <target name="compile" depends="copy_src">
            <echo message="${ant.project.name}: ${ant.file}"/>
            <javac debug="true" 
                destdir="${javac_dir}/WEB-INF/classes" 
                includeantruntime="false" 
                encoding="UTF-8"
                source="${source}" target="${target}">
                <src path="${javac_dir}"/>
                <classpath refid="XYSK.classpath"/>
            </javac>
        </target>

        <target name="war" depends="compile">
            <tstamp>
                <format property="TODAY" pattern="yyyyMMdd"/>
            </tstamp>
            <delete dir="${war}/${TODAY}"/>
            <mkdir dir="${war}/${TODAY}"/>
            <copy todir="${deploy_dir}/WEB-INF/classes" overwrite="true">
                <fileset dir="${javac_dir}/WEB-INF/classes">
                    <include name="**/*.class"/>
                </fileset>
            </copy>
            <war destfile="${war}/${TODAY}/XYSK.war" webxml="${deploy_dir}/WEB-INF/web.xml">
                <fileset dir="${deploy_dir}"/>
            </war>
        </target>
              	

    <target name="clean">
        <delete dir="WEB-INF/classes"/>
        <delete dir="work"/>
    </target>
    <target depends="clean" name="cleanall"/>
    <target depends="build-subprojects,build-project" name="build"/>
    <target name="build-subprojects"/>
    <target depends="init" name="build-project">
        <echo message="${ant.project.name}: ${ant.file}"/>
        <javac debug="true" debuglevel="${debuglevel}" destdir="WEB-INF/classes" includeantruntime="false" source="${source}" target="${target}">
            <src path="WEB-INF/src"/>
            <classpath refid="XYSK.classpath"/>
        </javac>
        <javac debug="true" debuglevel="${debuglevel}" destdir="work" includeantruntime="false" source="${source}" target="${target}">
            <src path="work"/>
            <classpath refid="XYSK.classpath"/>
        </javac>
    </target>
    <target description="Build all projects which reference this project. Useful to propagate changes." name="build-refprojects"/>
    <target description="copy Eclipse compiler jars to ant lib directory" name="init-eclipse-compiler">
        <copy todir="${ant.library.dir}">
            <fileset dir="${ECLIPSE_HOME}/plugins" includes="org.eclipse.jdt.core_*.jar"/>
        </copy>
        <unzip dest="${ant.library.dir}">
            <patternset includes="jdtCompilerAdapter.jar"/>
            <fileset dir="${ECLIPSE_HOME}/plugins" includes="org.eclipse.jdt.core_*.jar"/>
        </unzip>
    </target>
    <target description="compile project with Eclipse compiler" name="build-eclipse-compiler">
        <property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
        <antcall target="build"/>
    </target>
 
</project>
